export enum ViewMode {
  Grid = 'grid',
  List = 'list',
}

export type City = {
  city: string,
  adminName: string,
  population: number,
};

export type Size = {
  height: number,
  width: number,
};
